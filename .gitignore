.venv/
.browser-profile/
.browser-profile*/
__pycache__/
*.pyc
state/
output/
logs/
config/local.yaml
config/*.local.yaml
.atlas-browser-manager.lock
atlas_run.lock
*.sqlite
*.sqlite3
*.sqlite-wal
*.sqlite-shm
*.lock
.pytest_cache/
*.egg-info/

# Reproducible Playwright MCP tool manifest: lock the version, never the tree.
tools/playwright-mcp/node_modules/
dist/
build/
.env
.env.*

# Private/generated data fixtures, backups and runtime support bundles
fixtures/real/
fixtures/generated/
backups/
support_bundles/

# Phase 1B private candidate evidence and Workspace imports — NEVER commit.
# Real resumes, profile exports, populated candidate ledgers, PII, and the
# raw C:\Atlas-Agent-Import package must stay local-only. Public Git may
# contain only schemas, validators, synthetic fixtures, and public policy.
config/private/
private/
imports/private/
candidate_data/
*.private.yaml
*.private.json
*.pdf
*.docx
docs/private/
