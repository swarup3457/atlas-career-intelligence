---
name: DEDUPLICATION
description: Deduplicates job listings discovered across multiple sources. Dedupe key column exists in SQLite schema (jobs.dedupe_key); dedupe rules pending Workspace Agent import.
status: SCAFFOLDED
version: 0.1.0
---

# DEDUPLICATION

Placeholder. See `atlas/persistence/sqlite.py` (`jobs.dedupe_key` column).
